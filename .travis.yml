sudo: false

branches:
  only:
    - master

language: node_js

node_js:
  - v8
  - v7
  - v6

before_script:
  - npm install -g gulp

script:
  - gulp

cache:
  directories:
    - $HOME/.node-gyp
    - $HOME/.npm
    - $HOME/.yarn-cache
    - node_modules
  yarn: true
