language: node_js

node_js:
  - v7
  - v6

before_script:
  - node --version
  - npm --version
  - npm install -g gulp

script:
  - gulp

cache:
  directories:
    - $HOME/.node-gyp
    - $HOME/.npm
    - node_modules
  yarn: true
