const ASSETS = ['css', 'js', 'img', 'font', 'media'];

const ASYNC_FOLDER = 'async';

const HOT_RELOAD = 'webpack-hot-middleware/client?noInfo=true&reload=true';

export { ASSETS, ASYNC_FOLDER, HOT_RELOAD };
